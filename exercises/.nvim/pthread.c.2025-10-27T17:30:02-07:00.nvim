#include <pthread.h>
#include <stdint.h>
#include <stdio.h>

void *thread_function() { return (void *)42; }

int main() {
  pthread_t thread = 0;
  pthread_create(&thread, 0, thread_function, 0);

  void *ret_val = 0;
  pthread_join(thread, &ret_val);
  printf("%ld", (uint64_t)ret_val);
}
