#include <fcntl.h>
#include <stdio.h>
#include <sys/stat.h>
#include <unistd.h>

int main() {
  int file = open("tmp", O_RDWR | O_CREAT); //, S_IRUSR | S_IWUSR);
  write(file, "this is a test", 14);
  while (1) {
    sleep(30);
  }
}
