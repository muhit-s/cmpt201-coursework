#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {

  char *buff = NULL;
  size_t len = 0;
  ssize_t num_char = getline(&buff, &len, stdin);

  printf("%zd: %s\n", num_char, buff);

  free(buff);
}
