#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>

int main() {

  char *buff = NULL;
  char size = 0;
  ssize_t num_char = getline(&buff, &size, stdin);

  printf("%d\n", num_char); 

  free(buff);

}
